namespace Snake {
    export class Snake {
        public constructor(debug: boolean = false) {

            let engine = new Kouky.KoukyEngine(undefined, "root", debug);
            
        }
    }
}